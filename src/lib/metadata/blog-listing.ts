import { buildLocalizedPageMetadata } from "./localized-page";

/**
 * @deprecated Use `buildLocalizedPageMetadata` from `./localized-page` directly.
 * This alias is retained for backward compatibility while consumers migrate.
 */
export const buildBlogListingMetadata = buildLocalizedPageMetadata;
